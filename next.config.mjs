/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['t4.ftcdn.net'],
  //   devIndicators: {
  //   buildActivity: false,
  // },
  },
};

export default nextConfig;
